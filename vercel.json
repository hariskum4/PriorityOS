{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build -w packages/types && npm run build -w packages/scoring-engine && npm run build -w packages/ai-prompts && npm run build -w packages/life-os && npx prisma generate --schema apps/api/prisma/schema.prisma && npm run build -w apps/api && cd apps/mobile && EXPO_PUBLIC_API_URL=/api npm run build:web",
  "installCommand": "npm install",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "regions": ["bom1"],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "includeFiles": "{apps/api/dist/**,apps/api/prisma/**,node_modules/.prisma/**,node_modules/@prisma/**,packages/*/dist/**}"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "30 1 * * *"
    },
    {
      "path": "/api/cron/weekly",
      "schedule": "0 3 * * 0"
    }
  ]
}
