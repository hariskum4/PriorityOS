{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd packages/types && npm run build && cd ../scoring-engine && npm run build && cd ../ai-prompts && npm run build && cd ../../apps/mobile && EXPO_PUBLIC_API_URL=https://priority-api-0mus.onrender.com npm run build:web",
  "installCommand": "npm install",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
