{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd packages/types && npm run build && cd ../scoring-engine && npm run build && cd ../ai-prompts && npm run build && cd ../../apps/mobile && npm run build:web",
  "installCommand": "npm install",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
